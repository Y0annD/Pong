#ifndef MUR
#define MUR

class Mur:Shape{
 public:
 Mur(int x, int y, int width, int height, int orientation, double coefficient):Shape(x,y,width, height, orientation), _coefficient(coefficient){}

  
 private:
  double _coefficient;
};

#endif
